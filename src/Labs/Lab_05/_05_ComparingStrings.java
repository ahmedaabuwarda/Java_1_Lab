/**
 * To change this template file, choose Settings | Editor | File and Code Templates
 * and change the template in the editor.
 */

package Labs.Lab_05;

public class _05_ComparingStrings {
    public static void main(String[] args) {

        System.out.println("\n Method \t\t\t\t\t Description" +
                "\n equals(s1) \t\t\t\t return true if the string equals s1." +
                "\n equalsIgnoreCase(s1) \t\t return true if the string equals s1. (case insensitive.)" +
                "\n compareTo(s1) \t\t\t\t return 0 if equals(s1), return int > 0 if greater than(s1), " +
                "return int < 0 if less than(s1)" +
                "\n compareToIgnoreCase(s1) \t same as compareTo(s1) but (case insensitive)" +
                "\n startsWith(prefix) \t\t return true if the string starts with s1." +
                "\n endsWith(suffix) \t\t\t return true if the string ends with s1." +
                "\n contains(s1) \t\t\t\t return true if the string contains s1.");

    }
}